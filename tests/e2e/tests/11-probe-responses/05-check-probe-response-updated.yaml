apiVersion: kuttl.dev/v1beta1
kind: TestStep
# Requires 03-patch-probe-response.yaml (adds GET /probe) and 04-wait-resolver-cache.yaml before this step.
commands:
  - command: sh ../../manifest/check_probe_response.sh updated $NAMESPACE
    namespaced: true
